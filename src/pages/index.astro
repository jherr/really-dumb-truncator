---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Really Dumb Truncator</title>
	</head>
	<body class="bg-black text-white flex flex-col gap-4 max-w-3xl mx-auto mt-10">
		<h1 class="text-3xl font-bold border-b-2 border-b-blue-300">Really Dumb Truncator</h1>
		<p class="text-xl">
			This is a really dumb truncator. It's a web-app that truncates text to a
			[first]+[length-2]+[last] format. It's not really useful. But here we are.
		</p>
		<div class="flex gap-2">
			<input type="text" id="text" placeholder="Enter text here"
			class="w-full h-12 px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-600 focus:border-transparent text-black"
		/>
		<div class="whitespace-nowrap text-4xl">is truncated to</div>
		<div id="truncated" class="whitespace-nowrap min-w-24 text-4xl font-bold"></div>
		</div>
		<p class="text-xl">
			FAQ
		</p>
		<ul class="list-disc ml-4 text-2xl">
			<li><span class="font-bold">Why?</span> Because I wanted to.</li>
			<li><span class="font-bold">What?</span> A website that truncates text to a [first]+[length-2]+[last] format.</li>
			<li><span class="font-bold">How?</span> Some JavaScript and such.</li>
			<li><span class="font-bold">Why not use a library?</span> Because I don't want to.</li>
			<li><span class="font-bold">Why not use a framework?</span> Because I don't want to.</li>
			<li><span class="font-bold">Were these bullet points generated by AI?</span> Yes. (Stop lying AI.)</li>
		</ul>

	</body>
	<script>
		const text = document.getElementById("text");
		const output = document.getElementById("truncated");

		text!.addEventListener("input", () => {
			// @ts-ignore
			const value = (text?.value ?? "").trim().replace(/\s/g, "");
			if(value.length < 3) {
				output!.innerText = value;
			} else {
				output!.innerText = value.slice(0, 1) + (value.length - 2) + value.slice(-1);
			}
		});
	</script>
</html>
